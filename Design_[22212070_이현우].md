# 3. Design 
<img width="1408" height="768" alt="unnamed" src="https://github.com/user-attachments/assets/d310a9ac-1c0b-4c01-83fc-e23eb0d3468b" />

|||
| --- | --- |
| **Title** | **Medicine Helper** |
| **Student No** | **22212070** |
| **Name** | **이현우** |
| **E-mail** | **lhw031103@yu.ac.kr** |

## [ Revision history ]
| Revision date | Version # | Description | Author |
| :--- | :--- | :--- | :--- |
| 2026/05/31 | 1.00 | First Draft | 이현우 |

---

## Contents
1. Introduction
2. Class diagram
3. Sequence diagram
4. State machine diagram
5. Implementation requirements
6. Glossary
7. References

---

## 1. Introduction

### 1) Summary
본 단계에서는 디지털 헬스케어 보조 프로그램인 'Medicine Helper'의 분석 단계에서 도출된 시스템 요구사항과 유스케이스 모델을 바탕으로, 실제 소프트웨어 구현을 위한 구체적인 설계를 서술할 목적이 있습니다. 분석 단계에서 정의된 도메인 모델 및 사용자 인터페이스 프로토타입을 기반으로 시스템의 아키텍처, 데이터베이스 구조, 클래스 간의 상호작용 및 외부 API 연동 방식을 명확히 정의할 것입니다.

### 2) System review
'Medicine Helper'는 10~20대 및 다중 약물 복용 노인층을 주요 대상으로 하여 의약품 부작용 및 오남용을 방지하기 위한 어플리케이션입니다. 본 설계 문서가 포괄하는 주요 시스템 구현 범위는 다음과 같습니다. 
* **약물 정보 제공 및 추천:** 공공데이터(Medicine API)를 통해 정확한 약의 효능과 부작용 정보를 제공하며, 사용자가 입력한 아픔 상태(증상)를 기반으로 적합한 약품 리스트를 제안하는 기능. 
* **개인화된 복약 관리 시스템:** 사용자의 개인 약 바구니(Personal Medication Cart) 구성 및 데이터베이스 연동 설계. 
* **이메일 복약 알림:** Gmail API를 활용하여 사용자가 설정한 스케줄에 맞춰 알림 이메일을 전송하는 백그라운드 처리 및 스케줄러(Notification Scheduler) 설계. 
* **카메라 기반 약품 스캔:** 스마트폰 카메라 하드웨어 제어 및 외부 OCR API를 연동하여 이미지에서 텍스트를 추출하고 약품명을 필터링하는 로직 설계. 

### 3) Design strategy
성공적인 프로젝트를 위해 다음과 같은 원칙을 바탕으로 설계를 진행할 것입니다.
* **모듈화 및 관심사 분리:** 사용자 계정 관리, 약품 검색 및 추천, 알림 스케줄러, OCR 처리 등 각 도메인 영역을 분리하여 유지보수성을 높입니다. 
* **외부 API 연동 최적화:** 공공데이터 API, Gmail API, OCR API 등 다수의 외부 서비스와 통신해야 하므로, 네트워크 지연이나 API 서버 장애 발생 시 사용자 경험을 해치지 않도록 예외 처리 및 비동기 통신 구조를 설계합니다. 
* **법적/의료적 책임 분리:** 의료법 준수를 위해 단순 약품 리스트 제안 기능으로 제한하며, 면책 조항 관리자(Legal Disclaimer Manager)를 통해 사용자에게 의학적 보조 도구임을 명확히 인지시키는 UI/UX 흐름을 시스템 설계에 반영합니다.

---

## 2. Class diagram

<img width="2145" height="1406" alt="image" src="https://github.com/user-attachments/assets/08fd4148-ade8-4c32-a5a4-a89277213312" />

### 클래스 명세 (Attributes & Operations)

**1. User**
1) Attribute
* `-id: String`: 로그인할때 쓰는 아이디
* `-password: String`: 비밀번호
* `-email: String`: 알림을 받을 이메일 주소
* `-age: int`: 사용자의 나이
2) Operation
* `+login(id: String, pw: String): boolean`: 입력받은 id, password를 이용해 로그인 성공 여부를 반환함
* `+register(userInfo: User): boolean`: 입력받은 사용자 정보를 DB에 등록하고 성공 여부를 반환함
* `+logout(): void`: 로그아웃함
* `+viewProfile(): User`: 내 정보(User 객체)를 화면에 띄우기 위해 반환함
* `+updateProfile(updatedInfo: User): void`: 내 정보를 전달받은 정보로 수정함

**2. Medicine**
1) Attribute
* `-itemSeq: String`: 제품기준코드
* `-itemName: String`: 제품명
* `-efcyQesitm: String`: 효능
* `-useMethodQesitm: String`: 사용법
* `-atpnQesitm: String`: 주의사항
* `-seQesitm: String`: 부작용
2) Operation
* `+getMedicineInfo(): Medicine`: 약의 기본 정보가 담긴 객체를 반환함
* `+displayMedicineDetails(): void`: 약의 효능, 용법 등을 화면에 상세하게 출력함

**3. PersonalMedicationCart**
1) Attribute
* `-listId: String`: 이 장바구니의 고유 식별 번호
* `-user: User`: 이 장바구니의 주인인 User 객체
* `-medicineList: Medicine[]`: 장바구니에 있는 Medicine 객체들의 배열
2) Operation
* `+addMedicine(med: Medicine): boolean`: 배열에 새로운 약을 추가하고 성공 여부를 반환함
* `+removeMedicine(medicineCode: String): boolean`: 배열에서 특정 코드를 가진 약을 삭제하고 성공 여부를 반환함
* `+viewCartList(): Medicine[]`: 배열 안에 있는 모든 약 리스트를 반환함

**4. HealthStateRecord**
1) Attribute
* `-recordId: String`: 이 건강 기록의 고유 식별 번호
* `-symptomsList: String[]`: 유저가 선택한 증상들의 배열
2) Operation
* `+saveState(symptoms: String[]): void`: 화면에서 입력받은 증상들을 배열에 저장함
* `+getRecordDetails(): HealthStateRecord`: 이전에 저장한 건강 기록 객체를 반환함

**5. CameraScanner**
1) Attribute
* `-cameraStatus: boolean`: 카메라가 꺼져 있는지 켜져 있는지 상태 (true/false)
* `-imageBuffer: File`: 방금 찍은 실제 이미지 파일 데이터
2) Operation
* `+checkCameraPermission(): boolean`: 유저의 카메라 접근 권한 여부를 반환함
* `+openCamera(): void`: 카메라를 켜서 렌즈 뷰를 활성화함
* `+captureImage(): File`: 화면을 캡쳐하여 이미지 파일을 반환함

**6. OCRProcessor**
1) Attribute
* `-ocrApiUrl: String`: 사진을 보낼 구글 Vision 등 외부 OCR API의 주소
* `-extractedText: String`: 사진에서 추출해낸 원본 텍스트 전체
* `-filteredKeyword: String`: 원본 텍스트에서 추출해낸 약 이름 키워드
2) Operation
* `+processImage(image: File): String`: 사진을 API로 던지고 받은 원본 텍스트를 반환함
* `+filterMedicineName(rawText: String): String`: 텍스트를 DB와 비교 필터링하여 약 이름을 반환함

**7. NotificationScheduler**
1) Attribute
* `-alarmTime: DateTime`: 유저가 약을 먹겠다고 지정한 시간
* `-targetEmail: String`: 알람을 보낼 유저의 이메일 주소
2) Operation
* `+setAlarm(time: DateTime, email: String): void`: 지정된 시간에 특정 이메일로 알람을 설정함
* `+triggerEmail(): boolean`: 지정된 시간에 Gmail API를 호출하여 메일 전송 성공 여부를 반환함
* `+cancelAlarm(): void`: 설정한 알람 스케줄을 취소함

**8. LegalDisclaimerManager**
1) Attribute
* `-disclaimerText: String`: 이 앱은 의료 보조 도구라는 경고문 내용
* `-isAgreed: boolean`: 유저가 동의했는지 여부 (true/false)
2) Operation
* `+showDisclaimer(): String`: 경고문 텍스트를 반환하여 화면에 띄움
* `+agreeDisclaimer(): boolean`: 유저가 확인 시 동의 상태를 true로 변경하고 결과를 반환함

**9. MedicineRecommender**
1) Attribute
* `-targetSymptoms: String[]`: 유저가 넘겨준 아픔 증상 배열
* `-recommendedList: Medicine[]`: 분석 결과로 유저에게 제안할 약들의 배열
2) Operation
* `+analyzeSymptoms(symptoms: String[]): void`: 전달받은 증상 데이터를 분석 및 세팅함
* `+matchMedicine(): Medicine[]`: 분석된 증상을 기반으로 DB와 매칭시켜 추천 약 배열을 반환함

---

## 3. Sequence diagram

시스템의 전체 기능을 상세히 파악할 수 있도록 7가지 핵심 시나리오(Use Case)에 대한 시퀀스 다이어그램을 작성하였습니다.

### 1) 회원가입 및 로그인 (User Registration & Login)
사용자가 시스템을 사용하기 위해 계정을 생성하고 로그인하는 일련의 과정입니다. 사용자가 회원가입을 요청하면 시스템은 데이터베이스를 통해 기존 가입 여부(아이디 중복)를 확인합니다. 중복되지 않은 경우 사용자 정보를 안전하게 저장한 뒤 회원가입 성공 메시지를 반환하며, 중복된 경우 에러를 반환하여 재입력을 유도합니다. 가입이 완료된 사용자가 아이디와 비밀번호를 입력해 로그인을 요청하면, 데이터베이스에서 정보 검증 과정을 거치며 성공 시 시스템 접근 권한을 부여받고 실패 시 오류 메시지를 출력하여 보안을 유지합니다.

<img width="1323" height="1520" alt="스크린샷 2026-05-31 012549" src="https://github.com/user-attachments/assets/23e3326b-4c81-44f7-adc8-1e460f102dee" />

### 2) 약품 직접 검색 (Manual Medicine Search)
사용자가 알고 있는 약품명을 직접 입력하여 해당 약의 상세 정보를 검색하는 흐름을 보여줍니다. 사용자가 검색창에 약품명을 입력하면, 시스템은 곧바로 외부 공공데이터 API(MedicineAPI)로 검색 요청을 보냅니다. API 응답이 성공적이면 내부 Medicine 객체에 속성값을 매핑하고 디스플레이 함수를 호출하여 사용자에게 약의 상세 정보(효능, 용법, 부작용 등)를 화면에 띄웁니다. 반면 약품이 존재하지 않거나 API 서버 오류가 발생한 경우, 예외 처리를 통해 사용자에게 검색 결과가 없습니다라는 명확한 피드백을 제공합니다.

<img width="2045" height="1522" alt="스크린샷 2026-05-31 012736" src="https://github.com/user-attachments/assets/34780eb2-7150-4f5b-8cbf-1a214467d702" />

### 3) 카메라를 이용한 약품 스캔 (Camera Medicine Scan)
스마트폰 카메라 하드웨어를 제어하여 약품 라벨을 스캔하고, OCR 기술을 통해 약품을 자동 식별하는 복잡한 과정입니다. 사용자가 스캔 버튼을 누르면 먼저 기기의 카메라 접근 권한을 확인합니다. 권한이 허용된 경우 카메라를 실행하여 이미지를 캡처하고, 해당 이미지를 외부 OCR API로 전송하여 텍스트 데이터를 추출합니다. 추출된 원본 텍스트는 내부 OCRProcessor를 통해 핵심 약품명 키워드로 필터링되며, 이 키워드를 바탕으로 Medicine API에 정보를 요청해 상세 정보를 사용자에게 반환합니다. 권한 거부, 이미지 인식 실패, 네트워크 오류 등 발생 가능한 다양한 실패(Alt) 시나리오에 대한 에러 핸들링 과정도 명확히 포함되어 있습니다.

<img width="1784" height="1563" alt="스크린샷 2026-05-31 012828" src="https://github.com/user-attachments/assets/25294dc2-bf18-44cb-9eab-ed4353b64909" />

### 4) 증상 기록 및 면책 조항 동의 (Symptom Recording & Disclaimer)
사용자가 현재 겪고 있는 아픔이나 증상을 시스템에 기록하고, 약품 추천을 받기 전 법적 면책 조항에 동의하는 필수 보안/법률적 절차입니다. 사용자가 증상을 선택해 저장을 요청하면 시스템은 HealthStateRecord 객체에 이를 안전하게 기록합니다. 이후 추천 로직이 실행되기 전, LegalDisclaimerManager가 개입하여 본 어플리케이션이 전문적인 의학적 진단을 대체할 수 없다는 경고 팝업(면책 조항)을 화면에 띄웁니다. 사용자가 이에 명시적으로 동의해야만 다음 추천 단계로 넘어갈 수 있으며, 거부할 경우 진행이 즉각 중단되어 시스템과 사용자를 법적 위험으로부터 보호합니다.

<img width="1928" height="1467" alt="스크린샷 2026-05-31 012907" src="https://github.com/user-attachments/assets/3f2b22ce-13c8-4f87-9b74-e59ac8b577be" />

### 5) 증상 기반 약품 추천 (Symptom-based Recommendation)
면책 조항 동의가 완료된 직후, 앞서 기록된 사용자의 증상 데이터를 바탕으로 적합한 약품을 도출해 내는 핵심 추천 알고리즘의 흐름입니다. 시스템은 HealthStateRecord에서 증상 리스트를 불러온 뒤 MedicineRecommender에 분석을 의뢰합니다. 추천 모듈은 Medicine API를 호출하여 증상 키워드와 매칭되는 약품 데이터를 탐색하고, 조건에 부합하는 약품들을 필터링하여 배열 형태로 반환합니다. 매칭되는 약품이 존재하면 사용자에게 추천 약품 리스트로 제공하며, 일치하는 데이터가 없거나 통신 오류가 발생한 경우 추천할 약품을 찾지 못했습니다라는 안내와 함께 프로세스를 안전하게 종료합니다.

<img width="2120" height="1538" alt="스크린샷 2026-05-31 012939" src="https://github.com/user-attachments/assets/c8d1c799-d71c-4abb-92bb-6c386004e7a2" />

### 6) 개인 약 바구니 추가 (Manage Personal Cart)
사용자가 앞선 과정들을 통해 검색하거나 추천받은 약품을 개인화된 데이터 공간인 약 바구니(Personal Medication Cart)에 보관하고 관리하는 과정입니다. 사용자가 특정 약품을 바구니에 담기 요청하면, 장바구니 객체는 현재 보관 중인 배열(medicineList)을 탐색하여 중복 여부를 1차적으로 검사합니다. 이미 존재하는 약품일 경우 중복 추가를 방지하기 위해 에러 메시지를 띄우고, 새로운 약품일 경우 배열을 업데이트한 후 성공 메시지를 반환합니다. 이후 사용자가 바구니 보기를 클릭하면 저장된 모든 리스트를 반환하여 화면에 출력해 줍니다.

<img width="1445" height="1605" alt="스크린샷 2026-05-31 013019" src="https://github.com/user-attachments/assets/de87af9e-d2ec-49d6-8fda-9f3bbb2bf290" />

### 7) 복약 알림 설정 및 이메일 전송 (Notification Setup & Trigger)
사용자가 약 바구니에 담긴 약품들의 복용 스케줄을 설정하고, 정해진 시간에 맞춰 Gmail API를 통해 실제 이메일 알림이 전송되는 자동화 과정입니다. 사용자가 알람 시간과 수신할 이메일 주소를 입력하면 NotificationScheduler가 이 정보를 등록하여 백그라운드 대기 상태로 들어갑니다. 지정된 예약 시간이 도래하면 스케줄러가 자동으로 Gmail API를 호출하여 이메일 전송을 트리거합니다. 메일 전송이 성공하면 사용자에게 정상적으로 알림이 도달하며, 만약 API 서버 오류 등으로 전송에 실패할 경우에는 시스템에서 이를 로깅하고 앱 내 자체 푸시 알림으로 대체하는 등 철저한 예외 처리 과정을 보여줍니다.

<img width="1821" height="1461" alt="스크린샷 2026-05-31 013041" src="https://github.com/user-attachments/assets/0870ed4b-27e7-4884-b54d-5bbc7f94d527" />

---

## 4. State machine diagram

### 1) Client System State Machine Diagram (클라이언트 관점)
사용자가 앱을 이용하면서 겪게 되는 주요 UI/UX 상태 변화입니다. 에러 상태 처리를 포함합니다.

<img width="2030" height="1594" alt="image" src="https://github.com/user-attachments/assets/9f93f315-90b8-4ecd-b088-9e8446875ae8" />


### 2) Server System State Machine Diagram (서버/API 연동 관점)
클라이언트의 요청을 받아 외부 API와 연동하여 데이터를 처리하는 서버 로직의 상태 변화입니다. API 통신 실패 및 예외 처리를 반영합니다.

<img width="2521" height="1596" alt="image" src="https://github.com/user-attachments/assets/f7d06270-06d3-42cb-8aa4-577b2f5323a6" />

---

## 5. Implementation requirements

본 시스템을 구현하기 위한 운영 및 개발 환경(Operating environments) 요구사항은 다음과 같습니다.

* **Client (Front-end)**
  * **Languages:** HTML5, CSS3, JavaScript (ES6+)
  * **Framework/Library:** 필요에 따라 React Native 또는 순수 웹(PWA) 형태로 스마트폰 하드웨어(카메라)에 접근할 수 있는 환경.
  * **Device Requirements:** 인터넷 연결이 가능한 스마트폰 (iOS 14+ 또는 Android 10+), 카메라 모듈 필수 탑재.
* **Server (Back-end)**
  * **Runtime/Language:** java spring boot
  * **Database:** 사용자 정보 및 약품 리스트 스케줄을 저장할 관계형 DB 
  * **Hosting:** Aws
  * **External APIs (3rd Party)**
  * **Medicine API:** 공공데이터포털(Data.go.kr) 의약품 상세정보/부작용 API
  * **OCR API:** Google Cloud Vision API 
  * **Email API:** Gmail API (OAuth 2.0 인증 기반)
* **Design & Collaboration Tools**
  * VS Code, Git/GitHub, StarUML

---

## 6. Glossary

* **Medicine API:** 공공데이터포털(data.go.kr) 등에서 제공하는 의약품의 기본 정보, 효능, 용법, 주의사항, 부작용 데이터를 JSON/XML 형태로 가져오기 위한 오픈 API.
* **Gmail API:** Google에서 제공하는 이메일 발송 서비스로, 앱 내에서 사용자의 이메일로 복약 알림을 자동으로 전송하기 위해 연동하는 API.
* **OCR (Optical Character Recognition) API:** 이미지 속에 포함된 텍스트 문자를 시스템이 읽을 수 있는 데이터로 변환해주는 기술로, 카메라로 약 이름이나 처방전을 스캔할 때 사용됨.
* **Personal Medication Cart (개인 복약 장바구니):** 사용자가 현재 복용 중이거나 추후 복용할 약품들의 리스트를 한곳에 저장해두고 관리하는 가상의 공간.
* **Legal Disclaimer (면책 조항):** 앱에서 추천하는 약품 리스트가 의학적 진단이나 처방이 아닌 '단순 정보 제공' 목적임을 명시하여, 무면허 의료행위를 방지하고 책임 소재를 명확히 하는 법적 경고문.
* **Notification Scheduler (알림 스케줄러):** 사용자가 설정한 복약 시간에 맞춰 자동으로 이메일 알림 이벤트(Trigger)를 발생시키는 서버 측 백그라운드 프로그램.

---

## 7. References

1. Gmail API (https://developers.google.com/workspace/gmail/api/reference/rest?hl=ko)
2. Medicine API - 공공데이터포털 (https://www.data.go.kr)
3. Google Cloud Vision OCR API Documentation
4. 이데일리 기사 “약 안먹고 못버티는데…어떤 약이 위험한지 모르죠” - 2026.01.06

